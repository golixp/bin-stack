apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openlist-cert
  namespace: default
spec:
  secretName: openlist-tls
  issuerRef:
    name: cloudflare-issuer
    kind: ClusterIssuer
  dnsNames:
    - PLACEHOLDER_OPENLIST_DOMAIN
