apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

secretGenerator:
  - name: cloudflare
    namespace: cert-manager
    envs:
      - cloudflare.env

  - name: cloudflare
    namespace: default
    envs:
      - cloudflare.env
